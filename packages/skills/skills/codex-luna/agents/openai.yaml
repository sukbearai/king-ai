display_name: Codex Luna
short_description: Plan in Codex, then execute with GPT-5.6 Luna.
default_prompt: Use $codex-luna in a Codex session to freeze the plan, delegate execution to GPT-5.6 Luna at max reasoning, then review and verify the result.
