# EVIDENCE - Robinhood Chain X 默认采集与 RPC 限流恢复

- Tier: 3
- SPEC: `docs/robinhood-chain-x-rpc-reliability-spec.md`, approved Revision 6
- Spec approval: user replied `批准 Revision 6` on 2026-09-01
- Source state: HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; dirty working tree intentionally retained because this task and the pre-existing watchdog work are not committed
- Toolchain: Node `v22.22.3`, pnpm `9.15.0`, Git `2.50.1`, repository-owned TypeScript, Biome, Node test, VitePress, and skill validation commands
- Final entry point: `pnpm lint`, focused Robinhood tests, `pnpm verify`, `git diff --check`, then isolated LaunchAgent/SQLite/log inspection
- Final fresh run time: 2026-09-01 10:19 AWST
- Independent verification: not performed

## SPEC To Evidence Mapping

| Scenario, Must NOT, or failure mode | Falsifying test or gate | Final result |
| --- | --- | --- |
| X collection defaults on but explicit opt-out remains | shadow-daemon default/opt-out tests and live `x_enabled=true` inspection | pass |
| X cannot block or create chain evidence | X single-flight, failure-isolation, Phase 2-before-X tests; live interleaved Phase 0/1 and X logs | pass |
| Every Phase 0/1 read RPC retries bounded transient provider failures | focused retry tests for validation, block number, block reads, timestamp, liquidity, and logs | pass |
| A failed or partial batch cannot advance its cursor | Phase 0 partial-fetch and Phase 1 exhausted-retry tests | pass |
| Phase 1 polls swaps only for stablecoin-eligible pools while retaining all discovered pools | focused transport assertion; live database retained 1,330 pools, 250 stablecoin-eligible at inspection | pass |
| Delivery remains shadow with no Telegram, LLM, wallet, signing, order, or trade path | isolated config has only Robinhood Chain settings, both delivery fields are `shadow`, Phase 2 reports `liveDeliveryAuthorized=false` | pass |
| RPC source error rate recovers | three or more post-restart Phase 0/1 successes; both health rows `ok`, `consecutive_failures=0` | pass for the observed window |
| Phase 1 backlog converges | three consecutive successful rounds advanced 980 blocks while target growth was 2,797, 3,049, and 2,393 | fail |
| 72-hour readiness closes | Phase 2 readiness state and field duration | unverified; state remains `collecting` |

## Final Gauntlet Results

| Layer | Exact command or workflow | Actual result | Evidence boundary |
| --- | --- | --- | --- |
| Lint | `pnpm lint` | 259 files checked; no fixes; exit 0 | Static style only |
| Focused behavior | `node --test packages/cli/dist/test/robinhood-chain.test.js packages/cli/dist/test/robinhood-chain-phase1.test.js packages/cli/dist/test/robinhood-shadow-daemon.test.js` | 42/42 passed, 0 failed | Fake transports cannot prove provider quotas or sustained throughput |
| Repository verify | `pnpm verify` | CLI 592/592; GUI worker 177/177; skill Node 11/11; skill Python 9/9; 12 skills validated | Does not prove the live public RPC or 72-hour field gate |
| Diff integrity | `git diff --check` | exit 0 | Does not review semantic correctness or separate task changes from pre-existing dirty work |
| Shadow restart isolation | `launchctl kickstart -k gui/501/dev.king-ai-robinhood-shadow`, then LaunchAgent/process inspection | shadow PID `44431` -> `72297`; original trade daemon stayed PID `97500` | Short restart observation only |
| Live Phase 0/1 health | SQLite `source_health` and cursor inspection across post-restart rounds | both sources `ok`, both failure counters 0; no new post-restart RPC failure log | Short observation window, not the configured 72 hours |
| Live Phase 1 throughput | cursor/target time series from 17:41:37 through 17:54:42 AWST | cursor +980 each round; lag 53,851 -> 55,668 -> 57,737 -> 59,150 | Demonstrates non-convergence under current Phase 0-serialized runtime |
| Live Phase 2 and safety gate | `phase2_runs`, `readiness_checks`, and isolated config inspection | revision `phase2-v7-rpc-throughput` produced two `ok` runs; readiness `collecting`; live delivery false | No live alert delivery or trading acceptance was attempted |
| RPC batch feasibility probe | read-only 20-block batch on both configured RPCs and 50-block batch on the primary | 20/20 results on both; primary 50/50 in 1,628 ms | Low-load feasibility only; not sustained provider acceptance |

## Layers Not Run As Specified

- UNAVAILABLE: mutation testing; the repository has no mutation-test dependency or command. Focused negative-path tests and the real field run do not provide equivalent mutant coverage.
- N-A: browser visual verification; this change has no GUI surface.
- N-A: package publication, Worker deployment, Telegram delivery, wallet, signing, order, or trade execution; none is authorized or part of the shadow-only SPEC.
- UNAVAILABLE: 72-hour readiness; the observation window in this run is minutes, and readiness remains `collecting`.

## Independent Verification

- Verifier and fresh-context status: not performed; no independent verifier was requested for Revision 3.
- Exact source state observed: not applicable.
- Inputs supplied: not applicable.
- Attacks performed: not applicable.
- Findings and grading: not applicable.
- Fixes after last verified state: the EVIDENCE report itself only; no implementation change after the final gauntlet.

## Dismissed Findings

- X `unknown` observations are not classified as collector success. They remain explicit per-account health states, and live runs recorded them without converting them into chain evidence.
- The historical pre-restart 429/403/fetch failures are not treated as Revision 3 failures. Post-restart log lines and current health rows are separated by the `stopped`/new `started` boundary.

## Structural Blind Spots

- Public RPC policies, latency, and quota behavior may change after this short field window.
- Phase 0 still performs one HTTP request per block in Revision 3 and cannot currently keep pace with observed chain growth; this also delays Phase 1 despite its improved processing time.
- No 72-hour continuous run, alert review sample, live delivery authorization, package publication, or remote deployment was completed.
- The working tree includes pre-existing watchdog changes. Nothing was staged, committed, pushed, reset, or cleaned in this revision.

## Honest Notes

- RED proved the pre-Revision-3 implementation did not retry Phase 1 validation reads, queried non-stable pools for swaps, and defaulted Phase 0 concurrency to 16.
- Revision 3 is locally green and reduced the observed RPC error rate to zero in the short post-restart window.
- Revision 3 does not satisfy backlog convergence. Proposed Revision 4 is recorded in the SPEC and requires separate approval before implementation.

## Revision 4 Field Addendum

- Source state after Revision 4 implementation: dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; no commit or push was performed.
- Final fresh local gauntlet (2026-09-01 07:56 AWST): `pnpm lint` passed; `pnpm verify` passed with CLI 596/596, GUI worker 177/177, skill Node 11/11, skill Python 9/9, and 12 skills validated; `git diff --check` passed.
- Field revision: `phase2-v8-phase0-batch`; shadow PID `36290`; original trade daemon PID `97500` unchanged.
- Phase 0: `status=ok`, `consecutive_failures=0`; lag samples decreased `236,787 -> 236,022 -> 235,376 -> 234,795`.
- Phase 1: `status=ok` on successful samples but lag samples increased `433,262 -> 433,488 -> 434,302`; each tick advanced 980 blocks while the target advanced faster.
- Phase 2: latest run `phase2-v8-phase0-batch|ok`; readiness `collecting`; `liveDeliveryAuthorized=false`.
- X: independent account pass observed `accounts=15 posts=72 health={"ok":15}`; no chain signal or delivery side effect.
- Revision 4 result: Phase 0 throughput repair passes its field criterion; overall backlog convergence fails because Phase 1 is now the limiting stage. Proposed Revision 5 is recorded in the SPEC and requires separate approval.
- Extended v8 observation: Phase 0 lag later reached `232,067` with `consecutive_failures=0`; Phase 1 emitted a bounded 429/403 failure (`consecutive_failures=1`) and then recovered to `status=ok`, but the latest successful lag was `435,231`. This confirms provider-pressure instability and insufficient Phase 1 catch-up capacity remain separate risks.

## Revision 5 Field Addendum

- Source state after Revision 5 implementation: dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; no commit or push was performed.
- Local evidence before the final field calibration: `pnpm lint`, `pnpm verify`, and `git diff --check` passed; the focused Robinhood suite passed 51/51. The full verification included CLI, GUI worker 177/177, skill Node 11/11, skill Python 9/9, 12 validated skills, and the aligned English/Chinese VitePress build.
- Field revision: `phase2-v9-phase1-catchup`; shadow PID `76335`; original trade daemon PID `97500` remained unchanged.
- Field configuration: Phase 0 uses `https://robinhood-rpc.publicnode.com`; Phase 1 uses `https://rpc.mainnet.chain.robinhood.com`; `catch_up_blocks_per_tick=2000`, `provider_cooldown_ms=30000`, `max_log_blocks_per_request=1000`, and `log_rpc_concurrency=2`.
- Request-pressure result: the final creation-log and non-V4 swap-query compression produced four observed consecutive successful Phase 1 batches without a new 429/403 at that final process boundary. Earlier v9 calibration attempts did record bounded 429/403 failures, and those failures remain counted in the same evidence epoch.
- Cursor result: the final successful sequence advanced `50,863,851 -> 50,865,831 -> 50,867,811 -> 50,869,791 -> 50,871,771`. The reported lag included `462,638 -> 463,212 -> 462,918 -> 463,985`, which is not three consecutive decreases. Each catch-up batch advances 1,980 net blocks because the 20-block reorg overlap is intentionally rescanned.
- Latest Phase 1 health at 2026-09-01 09:42 AWST: `status=ok`, endpoint `https://rpc.mainnet.chain.robinhood.com`, target `51,335,756`, cursor `50,871,771`, lag `463,985`, and `consecutive_failures=0`.
- Latest Phase 2 readiness at the same boundary: `collecting`; observation 1.23 hours; 12 total runs; 7 successful runs; source error rate `0.4167`; 3,413 audited windows; 0 reviewed alerts; `liveDeliveryAuthorized=false`.
- Revision 5 result: local source and bounded-failure behavior are green, and the final short window improved the provider error rate. The required backlog-convergence field gate failed, so Revision 5 is not closed and the 72-hour readiness gate has not started from a qualifying healthy epoch.
- Remaining boundary: further increasing the catch-up limit above 2,000 or changing Phase 0/Phase 1 scheduling is outside the approved Revision 5 contract. Proposed Revision 6 is recorded in the SPEC and requires separate approval.
- Final post-addendum verification at 2026-09-01 09:49 AWST: `pnpm lint` passed with 259 files checked; `pnpm verify` exited 0 after rebuilding the CLI, GUI worker, and English/Chinese VitePress site and passing the CLI, GUI worker 177/177, skill Node 11/11, skill Python 9/9, and 12-skill validation suites. `git diff --check` and the repository decision validator also passed.

## Revision 6 Field Addendum

- Approval and source state: user approved Revision 6 on 2026-09-01. Implementation remains in the dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; no commit or push was performed.
- RED: the new disjoint-endpoint concurrency test failed against the Revision 5 implementation because only `phase0:start` was observed. The shutdown-drain test remained pending because serial scheduling never started Phase 1 while Phase 0 was blocked.
- Implementation: due Phase 0 and Phase 1 collectors run concurrently only when their sanitized RPC endpoint sets are disjoint. Overlap, inheritance, credential variants, query variants, and invalid URLs keep the sequential cooldown path. Phase 2 waits for both results; one stage failure does not cancel the other; shutdown starts no later work and drains existing chain and X tasks before releasing the PID lock.
- Focused evidence: the final Robinhood Phase 0/1/2/shadow-daemon command passed 66/66 tests, including concurrency, overlap fallback, Phase 2 barrier, failure isolation, and shutdown drain behavior.
- Local repository evidence: `pnpm lint` passed with 259 files checked; `pnpm verify` exited 0 after rebuilding the CLI, GUI worker, and English/Chinese VitePress site and passing all workspace test and skill-validation stages. `git diff --check` and the decision validator passed.
- Field rollout: the isolated LaunchAgent restarted from PID `76335` to PID `48861`; the original trade daemon stayed PID `97500`. The config started field revision `phase2-v10-rpc-parallel` and retained shadow delivery, X default-on, the 2,000-block Phase 1 catch-up limit, and separate Phase 0/Phase 1 RPC lists.
- Assembled behavior: post-restart logs repeatedly recorded `chain stages parallel rpcSets=disjoint`, with Phase 0 and Phase 1 persistence before the following Phase 2 persistence. No post-boundary 429/403 failure was observed.
- Phase 0 throughput: after the first warm-up sample, lag declined `212,994 -> 212,873 -> 212,008 -> 211,421`; latest status was `ok` with `consecutive_failures=0`.
- Phase 1 throughput: after the first warm-up sample, lag declined `465,988 -> 465,264 -> 465,131 -> 464,268 -> 463,681`; latest status was `ok` with `consecutive_failures=0`.
- Revision 6 result: the required short-window field criterion passes because both chain stages produced at least three consecutive lag decreases without renewed 429/403 errors. This closes the Revision 6 scheduling and backlog-convergence gate, not the overall Phase 2 readiness program.
- Remaining gate: `phase2-v10-rpc-parallel` has 2/2 successful runs over 0.11 hours, source error rate 0, 3,844 audited windows, 0 reviewed alerts, readiness `collecting`, and `liveDeliveryAuthorized=false`. The 72-hour, 800-run, review, and separate delivery-approval requirements remain open.

## Revision 7 Local And Field Addendum

- Approval and source state: Revision 7 source/tests/SPEC/decision/docs work was approved on 2026-09-01. The target-specific shadow migration/config/restart checkpoint was approved separately. The working tree remains dirty on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; no commit or push was performed, and unrelated watchdog changes were preserved.
- RED/GREEN: the initial focused run passed 50/53 and failed the three new dual-lane assertions. The final focused compiled Phase 0/Phase 1/Phase 2/shadow-daemon suite passed 78/78, covering legacy migration, realtime-first ordering, independent backfill health, prolonged-stop tip recovery, exact history closure, pool-cascade safety, boundary-window provenance, Phase 2 backfill exclusion, and daemon drain.
- Final local gauntlet: `pnpm lint` passed; `pnpm verify` exited 0 with CLI 616/616, GUI worker 177/177, skill Node 11/11, skill Python 9/9, and 12 skills validated. `git diff --check` and the decision validator passed before the evidence-only addendum.
- Preflight and backup: isolated shadow PID `48861`, original trade daemon PID `97500`, three SQLite `quick_check=ok`, approximately 490 GiB free. The shadow LaunchAgent was unloaded. It exceeded the first 60-second drain wait but exited before the write boundary; no database handles remained and the three databases rechecked `ok`. The backup under `~/.king-ai-robinhood-shadow/backups/revision7-dual-lane-20260901T113912+0800` reconciled 567,635 Phase 0 blocks, 4,868 Phase 1 audits, and 180 Phase 2 runs. The main database SHA-256 values were `dbcc2d172e27ed6f0ac34beb92ba8ee87413ccf4d0d8e66ebc771cca80dcbd48`, `c73079e713ffd6271784b7df8a83b713ad7fc885cd819de7fe728d759f2aff45`, and `03cdcf3f5b1da20beddec39d42e6c1f7df0841ab17dd614a13a13c3f47e2d187`.
- Migration: `signal_audit.collection_lane TEXT NOT NULL DEFAULT 'legacy'` was added through the built repository store path. All 4,868 existing audit rows were retained as `legacy`; pool, event, window, audit, candidate, and account-post counts were unchanged; post-migration `quick_check=ok`.
- Field config and process isolation: Phase 0 and Phase 1 both use `backfill_collect_seconds=300`; Phase 2 revision is `phase2-v11-dual-lane`; delivery remains `shadow`. The restarted sidecar is PID `57946`; the original trade daemon remained PID `97500`. Telegram, LLM, wallet, signing, order, and trading configuration remained absent.
- Freshness result: Phase 0 first reached realtime cursor `51,407,137` and Phase 1 `51,407,141`, both at confirmed-tip lag 0. A later cycle reached `51,409,618` and `51,409,624`, again with zero realtime failures. Phase 1 created realtime provenance while retaining old legacy audits; Phase 2 persisted one v11 `ok` run with realtime-only audited windows, zero v11 alerts, and zero duplicate pool/window/revision groups. No post-restart 429, 403, or Phase 0/1 failure was observed in the captured boundary.
- Backfill result: Phase 0 advanced the historical cursor by 1,980 blocks in each observed due backfill, but its coverage boundary also rebased; the observed computed gap was 213,832 at the final sample. Phase 1 first advanced `50,929,191 -> 50,931,171` with backfill gap 474,970, then advanced to `50,933,151`; ordinary slow realtime cycles also moved `realtime_start_block`, and the final observed gap increased to 480,962. `history_complete` remained false.
- Verdict: Revision 7 successfully restores current trend observation and separates realtime/backfill health and provenance, but it fails the historical-backlog closure objective under the live Phase 1 workload. The failure is deterministic capacity pressure: roughly 1,024 observed stable pools, including 964 V4 pools split into about ten topic groups, make a 1,000-block realtime scan slower than chain growth. Proposed Revision 8 must reduce the realtime request surface and permit tip rebase only at a true process-start recovery boundary. The decision remains `proposed`; the 72-hour readiness gate and live delivery remain closed.

## Revision 8 Local Addendum

- Approval and scope: the user approved Revision 8 on 2026-09-01. The approval covered source, tests, SPEC/decision records, aligned English/Chinese documentation, and disposable SQLite verification only. No existing shadow config or SQLite was modified, shadow PID `57946` and formal trade PID `97500` were not restarted or touched, and no commit, push, deployment, delivery, wallet, signing, order, or trade action was performed.
- Source state: dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; unrelated watchdog work remained present and was not reset, cleaned, staged, or overwritten.
- RED: after correcting the package filter to `@suwujs/king-ai`, the compiled new tests failed for the intended reasons: Phase 0 and Phase 1 did not reject same-process capacity overruns, and the V4 mixed response threw on a known non-stable pool instead of filtering it. The first combined RED run was manually interrupted after these failures because the new daemon test fixture still used a real 30-second outer wait; the fixture was changed to a deterministic clock without changing production behavior.
- Implementation: Phase 0 and Phase 1 accept additive `allowRealtimeRebase`. A required tip jump with this permission false throws `realtime capacity exceeded` before data, realtime cursor, backfill cursor, coverage boundary, or history state writes; the existing catch path records realtime health error. Standalone/manual calls default to recovery permission. Both long-lived owners, the shadow daemon and ordinary trade daemon, grant permission only to each collector's first process attempt and consume it after a failed attempt.
- V4 request surface: Phase 1 issues one verified PoolManager + swap-topic query per bounded log block chunk, then decodes only known stablecoin pools. Known non-stable pools and valid unknown pool keys are discarded. Wrong PoolManager/topic, malformed pool keys, and conflicting registered pool identity fail closed before cursor advancement. Non-V4 grouping, backfill ordering and limits, single SQLite writer, lane provenance, Phase 2 realtime-only materialization, X isolation, and shadow delivery remain unchanged.
- Test sensitivity: a temporary negative control forced every shadow cycle to receive rebase permission. The focused lifecycle test failed with actual permissions `[true, true]` versus expected `[true, false]`. The exact source line was restored before the final fresh gauntlet.
- Final fresh local gauntlet at 2026-09-01 12:36 AWST: `pnpm lint && pnpm verify && node --test packages/cli/dist/test/robinhood-chain.test.js packages/cli/dist/test/robinhood-chain-phase1.test.js packages/cli/dist/test/robinhood-chain-phase2.test.js packages/cli/dist/test/robinhood-shadow-daemon.test.js && python3 /Users/fayon/.codex/skills/usg-engineering-decision/scripts/verify_decisions.py --root /Users/fayon/workspace/github/pnpm/king-ai && git diff --check` exited 0.
- Numeric results: lint checked 259 files with no fixes; workspace verify rebuilt CLI, GUI worker, and VitePress docs; CLI passed 621/621, GUI worker 177/177, skill Node tests 11/11, skill Python tests 9/9, and 12 skills validated. The final focused Robinhood command passed 83/83. Decision validation passed 3 records and `git diff --check` exited 0.
- Evidence limits: fake RPC and disposable SQLite tests prove the bounded state transitions and request filtering they exercise, not public-provider throughput or target sidecar convergence. Revision 8 has no deployed or field evidence. The decision remains `proposed`; a separately authorized config/build/restart checkpoint is still required before checking three fresh realtime completions, stable same-process `realtime_start_block`, two monotonically decreasing Phase 0/1 backfill gaps, and absence of 429/403, duplicate audits, or duplicate drafts.
- Assurance boundary: repository mutation tooling is unavailable. The targeted negative control substitutes only for the first-attempt lifecycle branch and does not provide general mutation coverage. Independent verification was not performed; the repository guidance states that Claude Opus 4.7 will review the work separately.
- First field correction: the first Revision 8 sidecar attempt at PID `59227` safely failed Phase 1 with `uniswap_v4 swap references an unknown pool`; realtime and history cursors did not advance for that failed batch. This proved that a topic-only PoolManager response legitimately contains valid pool keys not present in the local registry. The implementation and tests were corrected to filter valid unknown keys while continuing to reject malformed or conflicting identities; the sidecar was unloaded before rebuilding.

## Revision 8 Field Addendum

- Final corrected source state: dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`; no commit or push was performed. An unrelated existing `runTg` serialization test exposed that its 2-second fake-process budget was below observed loaded-host startup time even though the ordered log assertion remained correct. Only that test budget was raised to 10 seconds; the separate 50-millisecond uncooperative-process deadline test and all production timeouts remained unchanged. The focused serialization test then passed 5/5 runs.
- Fresh local evidence at 2026-09-01 12:55 AWST: `pnpm lint && pnpm verify` exited 0 after building CLI, GUI worker, and VitePress docs; CLI passed 621/621, GUI worker 177/177, skill Node tests 11/11, skill Python tests 9/9, and 12 skills validated. The corrected focused Robinhood command passed 83/83. The decision validator and `git diff --check` also passed.
- Field authorization and isolation: the user separately authorized the field build, shadow-sidecar restart, and historical convergence observation. The consistent pre-existing backup is `~/.king-ai-robinhood-shadow/backups/revision8-fixed-rebase-20260901T124333+0800`; its three SQLite databases had `quick_check=ok`. The corrected LaunchAgent started as PID `20404` with `phase2-v12-fixed-rebase-v4-topic`. The formal trade daemon remained PID `97500`, and no live delivery, wallet, signing, order, or trade path was enabled or changed.
- First-cycle result: the sidecar started at 12:59:02 AWST. Phase 0 committed realtime cursor `51,453,440`, fixed start `51,451,441`, and backfill cursor `51,219,073` against target `51,451,440`. Phase 1 committed realtime cursor `51,453,522`, fixed start `51,452,523`, and backfill cursor `50,947,011` against target `51,452,522`. Each backfill advanced 1,980 net blocks. The restart recovery boundary increased the new fixed gaps to 232,367 for Phase 0 and 505,511 for Phase 1; field acceptance evaluates monotonic decrease only across later due cycles in the same process.
- Capacity result: after the first combined realtime/backfill/Phase 2 cycle, the next same-process attempt saw Phase 0 require 3,216 blocks with capacity 2,000 and Phase 1 require 3,132 blocks with capacity 1,000. Both failed closed with `realtime capacity exceeded` before moving realtime cursor, backfill cursor, or `realtime_start_block`. A later Phase 0 target reached `51,457,241` and Phase 1 target `51,457,271`; their fixed starts and cursors remained unchanged. No repeated rebase occurred.
- Workload evidence: the successful 1,000-block Phase 1 realtime range contained 204 retained swaps across 178 distinct blocks and 20 active pools, including 194 Uniswap V4 swaps across 168 blocks. The implementation still resolves timestamps through individual `eth_getBlockByNumber` calls. The entire first cycle, including a due 2,000-block historical pass and the Phase 2 barrier, delayed the next realtime attempt until the chain had advanced beyond both bounded capacities. This identifies the remaining bottleneck as realtime scheduling and per-block metadata throughput rather than V4 pool-topic fan-out.
- Integrity and duplication: the sidecar was booted out after the deterministic capacity failures and drained normally before its PID lock was released. All three live SQLite databases returned `quick_check=ok`. Phase 1 contained 5,693 audits with 0 duplicate `audit_id` values and 0 duplicate pool/window groups. The v12 epoch contained two Phase 2 runs, one `ok` and one `source_error`, zero v12 alerts, and zero duplicate alert IDs. No post-boundary 429 or 403 was observed.
- Verdict: Revision 8 proves first-attempt rebase ownership, fixed same-process history boundaries, V4 unknown-key filtering, and atomic capacity failure in the target sidecar. It fails the approved field gate because there were not three consecutive realtime completions and no second due backfill cycle could run. The shadow LaunchAgent is stopped, the formal trade daemon remains PID `97500`, the decision stays `proposed`, and the 72-hour readiness and live-delivery gates remain closed.
- Revision 9 pre-proposal probe: with the sidecar stopped, a read-only two-item `eth_getBlockByNumber` JSON-RPC batch was sent to the configured Phase 1 endpoint. It returned an array of two results with the exact requested IDs and block numbers and no per-item error. This proves only that the target endpoint accepts a small native batch; it does not prove the proposed implementation, larger configured batch size, sustained throughput, or convergence.

## Revision 9 GMGN Pre-Proposal Evidence

- Capability inspection: the current official GMGN CLI package inspected for this proposal recognizes `robinhood` as a chain and exposes read-only trending, trenches, token information/security/pool/holder/trader, and K-line commands. Revision 9 nevertheless proposes a repository-owned HTTP adapter so the production trust boundary does not depend on a mutable global CLI installation.
- Credential boundary: the local GMGN environment contains separately named API-key, private-key, and clock-offset variables. No values were printed or persisted. The successful probes used only the API key; the proposed collector must read only `GMGN_API_KEY` and must exclude `GMGN_PRIVATE_KEY` from its production and test capability surface.
- Clock failure reproduction: an unmodified CLI read request failed with HTTP 401, `AUTH_TIMESTAMP_EXPIRED`, because the local system clock was approximately 105 seconds behind GMGN's HTTPS `Date` header while authentication accepts only a narrow timestamp window. The inspected CLI signs with `Date.now()` and did not apply the configured offset. This is the direct RED evidence for server-date-derived, bounded in-memory clock correction without changing the OS clock.
- Trending live probe: a read-only authenticated `GET /v1/market/rank?chain=robinhood&interval=1h&limit=3`, signed with the GMGN server-date offset and a UUID client id, succeeded and returned three Robinhood records. The response used a nested envelope whose inner successful payload was `data.rank`, not the initially assumed flat array. Observed fields included address, chain, symbol, price, volume, swaps, liquidity, market cap, holder count, creation timestamp, launchpad metadata, smart-degen/renowned counts, honeypot/wash-trading flags, concentration fields, and social-risk fields.
- Trenches live probe: a read-only authenticated `POST /v1/trenches?chain=robinhood` requesting `new_creation`, `near_completion`, and `completed` succeeded. The response contained those category arrays, but `new_creation` returned 60 rows even though the request body asked for a limit of three. Observed records included token and pool addresses, created/open timestamps, launchpad status, liquidity, market cap, 24-hour volume/swaps, holder count, security/tax flags, holder/insider/bundler/sniper/bot/fresh-wallet metrics, and social links/duplication flags. This proves the need for a client-side hard cap.
- Unsupported surface: a read-only `token_signal` probe returned HTTP 400 `BAD_REQUEST`; it is excluded from the required Revision 9 endpoint set. No swap, signing, wallet, order, cooking, portfolio, delivery, or trading endpoint was called.
- Evidence boundary: these probes prove current read-only Robinhood response availability, the observed envelope shapes, the client-limit defect, and the clock-skew failure. They do not prove the proposed adapter, persistence, deduplication, candidate scoring, Phase 2 integration, long-running quotas, field freshness, or rollback. No daemon was started or restarted, no existing SQLite file was written, and the formal trade daemon remained outside the probe scope.

## Revision 9 Local Implementation Addendum

- Approval and scope: the user approved Revision 9 on 2026-09-01. Approval covered source, tests, the repository-owned mutation runner, configuration example, SPEC/EVIDENCE/decision records, aligned English/Chinese documentation, and disposable SQLite verification only. It did not authorize reading `GMGN_PRIVATE_KEY`, changing an existing shadow configuration or database, restarting a daemon, commit, push, deployment, paid operations, live delivery, wallet access, signing, swaps, orders, or trading. None of those actions was performed.
- Source state: dirty working tree based on HEAD `c500a030350b69de74061cb7a29e2d55def0d53a`. The pre-existing watchdog and other unrelated working-tree changes were preserved without reset, cleanup, staging, or overwrite. No commit or push was performed.
- Implementation: `discovery_source="gmgn"` activates a repository-owned read-only adapter pinned to `https://openapi.gmgn.ai`. It reads only `GMGN_API_KEY`, uses the GMGN HTTPS `Date` header for bounded in-memory clock correction, performs one timestamp-expired replay, and bounds 429, 5xx, transport, deadline, and cancellation behavior. It collects `1m`, `5m`, and `1h` trending plus the three trenches categories, applies client-side result caps, fails closed on missing required market/risk fields, merges provenance deterministically, and writes one tick atomically to the independent `gmgn-v1` SQLite store.
- Candidate and authority boundary: a fresh qualifying `5m` record requires same-window `1m` or trenches corroboration. RPC checks Chain ID `4663` and bytecode for at most 20 unique shortlisted addresses; it cannot discover or create a candidate. Phase 2 uses `phase2-v13-gmgn-primary`, source-agnostic subject identity, verified GMGN rows only, and exact-address X enrichment. GMGN mode skips legacy Phase 0/Phase 1 discovery while preserving all legacy databases and cursors for rollback/audit.
- Focused evidence: the compiled Robinhood Phase 0, Phase 1, GMGN, Phase 2, and shadow-daemon command passed 105/105 tests. The GMGN and Phase 2 subset passed 35/35, including exact nested envelopes, per-category trenches caps, unknown-field rejection, 105-second clock correction, timestamp refresh, bounded post-refresh rate limiting, API-key redaction, credential isolation, strict Chain ID parsing, 20-address RPC enforcement, RPC cancellation before retry, tick atomicity, scheduler drain, v13-only materialization, and rejection of unverified or legacy evidence.
- Test sensitivity: `node scripts/verify-robinhood-gmgn-mutations.mjs` killed eight non-equivalent defects: increasing the RPC address cap from 20 to 21, accepting a Chain ID with an invalid suffix, counting timestamp refresh against the normal retry budget, accepting a missing nested trending status, starving later trenches categories, ignoring RPC verification cancellation, accepting unknown honeypot status, and materializing unverified GMGN candidates. The runner restored exact source bytes and the focused suite passed after restoration.
- Final fresh local gauntlet on 2026-09-01 AWST: `pnpm lint`, `pnpm verify`, the focused Robinhood command, the mutation runner, a separate VitePress docs build, the repository decision validator, production secret/capability scans, and `git diff --check` all exited 0. Lint checked 262 files; workspace verification passed CLI 643/643, GUI worker 177/177, skill Node 11/11, skill Python 9/9, and 12 validated skills. Decision validation passed four records. Production source contained no `GMGN_PRIVATE_KEY` or `X-Signature`; the GMGN module's write-capability keyword scan found no wallet, signature, signing, swap, order, or trading capability.
- Field boundary: no live GMGN collector tick was executed from this implementation, no sidecar was built or restarted, and no existing SQLite file or live process was changed. Three consecutive online GMGN ticks, all six fresh feed categories, live bounded RPC verification, duplicate-free field persistence, v13-only field readiness, 72 hours, 800 runs, ten reviews, and a separate live-delivery approval all remain unverified. The decision therefore remains `proposed`.

## Revision 10 Local Implementation Addendum

- Tier and approval: Tier 3 Revision 10 of `docs/robinhood-chain-x-rpc-reliability-spec.md` was approved when the user replied `批准 Revision 10 SPEC` on 2026-09-01. Approval covered source, tests, additive disposable SQLite, the configuration example, aligned English/Chinese documentation, SPEC/EVIDENCE/decision records, and the local gauntlet. It did not authorize changing the existing shadow configuration or SQLite, injecting field Telegram credentials, restarting either daemon, committing, pushing, wallet access, signing, swaps, orders, or trading. None of those separately controlled actions was performed.
- Source state: the final local evidence targets the task-only dirty working tree based on `main` HEAD and `origin/main` `f3b5e127a7ea9bb1f950b252dcacb18fe37deb74`. No unrelated pre-existing change was discovered, and no file was staged, committed, pushed, deployed, or loaded into the field sidecar.
- RED: the first approved parser test failed compilation because `RobinhoodPhase2Config` lacked `telegramSubjectCooldownSeconds`. The later behavioral tests initially had no delivery API and therefore could not pass. During mutation development, changing the omitted `delivery` default from `shadow` to `telegram` survived the first focused suite, exposing a missing compatibility assertion; the omitted-field test was added without changing the approved behavior, and the mutant was then killed.
- Implementation and authority: Phase 2 accepts `delivery="shadow" | "telegram"` while omission remains `shadow`. Only `runRobinhoodShadowDaemon`, after a committed `status="persisted"` Phase 2 result, invokes the automatic delivery owner. Manual materialization, status, review, the GMGN collector, and the ordinary trade scheduler do not send. The existing readiness metrics remain visible, while `liveDeliveryAuthorized` reports explicit delivery configuration rather than a trading grant.
- Durable lifecycle: additive `telegram_delivery_metadata`, `telegram_deliveries`, and `telegram_subject_cooldowns` tables record one enablement boundary per field revision, suppress existing current drafts, seed subject cooldown, claim each alert as `sending` before the external request, retry only known failures with bounded exponential backoff, and convert interrupted or restart-observed `sending` to terminal `unknown`. Network I/O occurs outside SQLite transactions. Current v13 verified GMGN drafts only are eligible; stale, legacy, X-only, RPC-only, oversized, already sent, cooldown-suppressed, and unknown rows do not send. Each cycle performs at most ten single-alert requests.
- Telegram boundary: `sendTelegramSingle` performs one request with the existing `telegramFromConfig` credential resolution, rejects oversized or missing-credential input before network I/O, distinguishes known HTTP failure from transport/abort ambiguity, and has no internal retry. Tokens and chat ids are not logged or persisted by the delivery ledger. Missing credentials affect only delivery and become observable `retry_wait`; they do not roll back GMGN or Phase 2 persistence.
- Focused evidence: the compiled command `node --test packages/cli/dist/test/robinhood-chain-phase2-telegram.test.js packages/cli/dist/test/robinhood-chain-phase2.test.js packages/cli/dist/test/robinhood-shadow-daemon.test.js packages/cli/dist/test/trade-telegram.test.js` passed 53/53 tests. It exercised nine automatic-delivery SQLite scenarios, seventeen surrounding Phase 2 scenarios, twenty-one shadow-daemon lifecycle scenarios, and six Telegram boundary scenarios.
- Mutation sensitivity: `node scripts/verify-robinhood-telegram-mutations.mjs` killed five non-equivalent defects: changing the omitted default to Telegram, baselining stale instead of current draft rows, raising the per-cycle network limit from ten to eleven, making terminal `unknown` retryable, and bypassing subject cooldown. The runner fails on missing or ambiguous source matches, restores exact source bytes after each mutant, and reruns the clean focused suite after restoration.
- Final fresh local gauntlet on 2026-09-01 AWST: `pnpm lint`, `pnpm verify`, the focused 53-test command, `node scripts/verify-robinhood-telegram-mutations.mjs`, a separate `pnpm --filter @king-ai/docs build`, the installed engineering-decision validator, production secret/capability ownership scans, and `git diff --check` all exited 0. Lint checked 264 files. Workspace verification passed CLI 657/657, GUI worker 177/177, skill Node 11/11, skill Python 9/9, and 12 validated skills. Decision validation passed five records.
- SPEC-to-evidence mapping: backward-compatible shadow default is pinned by parser and non-delivery tests; no-history flooding and cooldown are pinned by disposable-database baseline tests; readiness-independent sending, single-request success, missing credentials, and Telegram HTTP/transport categories are covered by module integration; retry, abort, post-send crash, restart, and no-later-send behavior cover lifecycle failure modes; stale/legacy/oversized filtering and ten-request bounds cover eligibility and resource limits; sidecar order and failure isolation prove the production owner boundary; mutation and secret/capability scans attack the corresponding Must NOT constraints.
- Layers not run: changed-line coverage tooling is unavailable and was not represented as passed; the focused mutation runner substitutes only for its five selected defects. Independent verification was not performed because repository guidance assigns the later review to Claude Opus 4.7. Real Telegram, existing-database migration, LaunchAgent shutdown/restart, wrapper credential injection, deployed logs, field delivery ledger, and receipt of a naturally eligible Telegram alert remain unverified.
- Field and database boundary: all schema writes were confined to task-owned temporary SQLite databases removed by their tests. No existing local or field database was opened through the new write path. Before field execution, the exact sidecar database and fixed runtime revision still require read-only preflight, a consistent backup, explicit migration/restart authorization, post-migration `quick_check`, baseline-count reconciliation, and rollback to `delivery="shadow"` if the delivery owner fails. The formal trade daemon remains outside scope, so the decision stays `proposed` until deployed evidence exists.

## Revision 11 Local Implementation Addendum

- Tier and approval: Tier 3 Revision 11 of `docs/robinhood-chain-x-rpc-reliability-spec.md` was approved when the user replied `批准 Revision 11 SPEC` on 2026-09-01. Approval covered the described source, focused tests, disposable SQLite, SPEC/EVIDENCE/decision updates, aligned English/Chinese documentation, mutation updates, and local gauntlet. It did not authorize modifying the current shadow configuration or SQLite, adding a GMGN endpoint, restarting a daemon, committing, pushing, deployment, wallet access, signing, swaps, orders, or trading. None of those actions was performed.
- Source state and toolchain: the verified tree is the task-only dirty `main` worktree based on HEAD and `origin/main` `f3b5e127a7ea9bb1f950b252dcacb18fe37deb74`, using Node `v22.22.3`, pnpm `9.15.0`, Git `2.50.1`, repository TypeScript/Biome/Node test/VitePress, and the installed decision validator. No file was staged, committed, pushed, deployed, or loaded into the field sidecar.
- Field-shape precondition: a read-only 2026-09-01 snapshot of `/Users/fayon/.king-ai-robinhood-shadow/trade/state/robinhood_chain_gmgn.sqlite` counted 26,326 observations. All returned `missing` for both `$.social_links` and `$.is_social_duplicate` in `evidence_json`; zero rows contained `social_links`. The query made no SQLite write. This proves the current persisted feed cannot support a hard account-presence gate and does not prove future GMGN shapes or account ownership.
- RED: after the approved tests were compiled against the pre-Revision-11 implementation, the focused command passed 33 and failed 6. The intended failures were missing normalized handles, absent five-point bonus, absent duplicate rejection, the still-v13 Phase 2 config, v13 materialization into the proposed v14 authority, and missing v14 message/epoch behavior. The failures were assertion failures in production parser/scorer/Phase 2 behavior, not import or fixture setup failures.
- Implementation: GMGN observation normalization accepts only bounded, explicitly X/Twitter-labelled profile evidence. HTTPS account URLs are limited to exact X/Twitter hosts, one valid account segment, no credentials, explicit port, query, fragment, status/share/product route, or invalid handle. Labelled objects/arrays and direct `@handle`/X URL values normalize to lowercase handles; arbitrary token text is not scanned. Missing or malformed data adds no score and does not reject. One unambiguous fresh same-window handle adds exactly 5 points capped at 100. An explicit boolean `is_social_duplicate=true` or multiple valid handles rejects the candidate; non-boolean truthy values and stale duplicate evidence are not authoritative.
- Authority and rendering: core `5m`, corroboration, market, risk, pool, and RPC verification remain mandatory. Project-X evidence cannot create or verify a candidate. GMGN authority is `phase2-v14-gmgn-project-x`; v13 observations/candidates/drafts/claims/cooldowns remain auditable but cannot count toward v14 readiness or automatic delivery. Phase 2 renders only a normalized plain-text `project_x=@handle (GMGN-declared)` line and never labels the account `official` or creates a link.
- Focused final evidence: `pnpm lint && pnpm --filter @suwujs/king-ai build && node --test packages/cli/dist/test/robinhood-chain.test.js packages/cli/dist/test/robinhood-chain-phase1.test.js packages/cli/dist/test/robinhood-chain-gmgn.test.js packages/cli/dist/test/robinhood-chain-phase2.test.js packages/cli/dist/test/robinhood-chain-phase2-telegram.test.js packages/cli/dist/test/robinhood-shadow-daemon.test.js packages/cli/dist/test/trade-telegram.test.js` exited 0. Lint checked 264 files; the focused command passed 127/127 with zero failures. Its GMGN/Phase 2/Telegram subset contributed 48/48.
- Mutation sensitivity: `node scripts/verify-robinhood-gmgn-mutations.mjs && node scripts/verify-robinhood-telegram-mutations.mjs` exited 0. The GMGN runner killed 14/14 non-equivalent defects, including reserved-route acceptance, ignored duplicate/conflict rejection, removed score bonus, reused v13 epoch, and an unsupported `official` label. The Telegram runner killed 5/5 delivery defects. Both runners fail on missing/ambiguous source matches, restore exact source bytes, and passed their clean focused suite after restoration. They do not constitute exhaustive mutation coverage.
- Repository final evidence: `pnpm verify` exited 0 after building CLI, GUI worker, and English/Chinese VitePress docs; CLI passed 661/661, GUI worker 177/177, and skill Node tests 11/11. A separate `pnpm --filter @king-ai/skills validate` passed the same 11 Node tests, 9/9 Python tests, and validated 12 skills. The installed decision validator passed 5 records. Production scans found no `GMGN_PRIVATE_KEY`, `X-Signature`, wallet/private-key/swap/order/trade route, and `git diff --check` exited 0.
- SPEC-to-evidence mapping: valid labelled shapes and hostile URLs map to GMGN normalizer table tests; soft bonus, missing/malformed compatibility, risk non-bypass, duplicate/conflict and input-order behavior map to candidate tests; v13 exclusion and exact declared-account rendering map to disposable GMGN/Phase 2 SQLite tests; no-X-only qualification remains pinned by the existing Phase 2 test; project-X parser/scorer/epoch/wording defects map to the six new mutation cases; the full and focused suites cover the surrounding automatic-delivery lifecycle.
- Unavailable or unperformed layers: changed-line coverage tooling is unavailable and was not represented as passed. Independent verification was not performed because repository guidance assigns later review to Claude Opus 4.7. No live v14 GMGN tick, new social endpoint, sidecar build/restart, existing-database write/migration, Telegram credential injection, real Telegram send, deployment, wallet, signing, swap, order, or trade was performed.
- Rollout boundary: the current field database contains v13 data and lacks project social fields. A later authorized rollout must back up the exact live databases/config/wrapper, build a fixed source state, start only `dev.king-ai-robinhood-shadow`, verify v14-only source health and candidate/readiness rows, confirm that v13 delivery metadata cannot leak into v14, and observe naturally supplied GMGN social evidence before claiming the project-account signal works online. The formal trade daemon remains outside scope.
